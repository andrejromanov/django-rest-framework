name: Bandit
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  security-scan:
    name: SecurityScan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Scan
        run: |
          ./run_bandit.sh